# VEIL Frontend Environment Configuration
# Copy this file to .env.local and modify as needed

# Network: "localnet" or "devnet"
# - localnet: Use with `arcium localnet` for local development
# - devnet: Use Solana devnet (default)
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Optional: Override RPC endpoint
# NEXT_PUBLIC_RPC_ENDPOINT=http://localhost:8899
# NEXT_PUBLIC_RPC_ENDPOINT=https://api.devnet.solana.com
